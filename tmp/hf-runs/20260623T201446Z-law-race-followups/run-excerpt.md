# AI Different Law-Race Follow-up Run

Run ID: `20260623T201446Z-law-race-followups`
Job: `6a3ae930d530f3857e66d934`
Branch: `codex/discovery-law-race-holdouts`
Final watched run: not run

## Key Outcomes

- `selected_law_conflict_resolution` on `repulsion`: `conflict_rival_supported`.
- `selected_law_conflict_resolution` on `inverse_square_repulsion`: `conflict_domain_split_supported`.
- `model_disagreement_probe` on `inverse_square_repulsion` repeated once and returned `rival_confirmed` then `target_confirmed`, pointing to a queue consolidation fix.
- Readiness printed as `100%`, `ready_for_watched_final`, `25/25` gates.

The original HF run completed, but uploading artifacts to the HF dataset failed with a protected-branch/write-permission 403. A recovery upload job also failed, so this recovered summary is persisted on the GitHub branch.
